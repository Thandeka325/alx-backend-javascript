export default function taskBlock(trueorFalse) {
  const task = false;
  const task2 = true;

  if (trueorFalse) {
    // Do nothing or perform operations without redeclaring variables
  }

  return [task, task2];
}
