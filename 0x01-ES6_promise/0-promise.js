export default function getResponseFromAPI() {
  return new Promise((resolve) => {
    // Can either use resolve or reject here based on logic
    resolve(); // For now, I chose to resolve it
  });
}
